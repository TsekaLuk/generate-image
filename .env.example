# generate-image skill credentials
# 复制本文件为 .env 后填入真实密钥；.env 切勿提交或泄露到聊天/日志。
# 只填你要用的 provider 的 key 即可（默认 auto 路由：按请求能力挑一个已配置的 provider）。
#
# 覆盖 base_url / 默认模型（可选，用于接入未预置的 OpenAI 兼容 provider）：
#   OPENAI_BASE_URL=...    OPENAI_DEFAULT_MODEL=...
#   OPENROUTER_BASE_URL=...    SILICONFLOW_DEFAULT_MODEL=...  等等

# OpenAI — 官方 Images API（默认 provider，默认模型 gpt-image-2.5-flare）。
# 2.5 有两个 id：-flare 更快（默认），-sunburst 编辑精度优先、更慢；gpt-image-2 仍可选。
# 也可以是任何支持 gpt-image 的 OpenAI 兼容中转：设 OPENAI_BASE_URL 指向中转地址即可。
# 注意：中转常常忽略 size（官方 API 不会），届时画幅只能靠 -r 的提示词钩子。
OPENAI_API_KEY=

# 302.AI — OpenAI 兼容聚合器（/v1/images/generations + /v1/images/edits）
AI302_API_KEY=

# OpenRouter — 统一网关（/v1/images；img2img 走 chat）
OPENROUTER_API_KEY=

# SiliconFlow / 硅基流动（/v1/images/generations，image_size 必填，返回 url）
SILICONFLOW_API_KEY=

# 火山方舟 Ark — Seedream 5.0 / 5.0 lite / 5.0 Pro（/api/v3/images/generations）
ARK_API_KEY=

# 147ai / nn.147ai.com（站名 Nano Banana）— 一个网关两套方言，按模型自动切换：
#   gemini-*      → /v1/chat/completions（extra_body.google.image_config，1K/2K/4K）
#   gpt-image-2-* → /v1/images/generations + /v1/images/edits（size 真生效）
# 本 skill 唯一能出真 4K 的渠道（GENIMAGE_IMAGE_SIZE=4K，需配 pro 模型）。
# 变量名不能以数字开头，故沿用 AI302_API_KEY 的命名惯例加 AI 前缀。
AI147_API_KEY=
